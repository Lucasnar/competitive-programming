#include<cstdio>

const int maxn = 1010;

int f2[maxn][maxn];
int f5[maxn][maxn];
int pen[maxn][maxn];

int main(){
    int n, x, m2, m5;

    scanf(" %d", &n);

    for(int i = 0; i<; ++i)
        for (int j = 0; j<n; ++j){
            scanf(" %d", &x);
            if(!x){
                f2[i][j] = 1000;
                f5[i][j] = 1000;
            }
            else {
                while(!
        }


    return 0;
}
